#!/bin/python3

def send_to(q_,receiver,type,val1,val2,val3)
	#use the correct queue to send (q_ is a set to define)
	#write in queue

def recv_from(q_,i):
	#use the correct queue to receive
	#read in queue
	return {"type" : t, "L" : L, "F" : F, "S" : S} #pour la forme
